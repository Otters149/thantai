from src.database_connector.database_connector import DBConnector
from src.logger import AppLogger

class DB_TABLE_NAME:
	SESSIONS = "sessions"
	USERS = "users"
	USER_INFOS = "userinfos"
	SUBSCRIPTION = "subscriptions"